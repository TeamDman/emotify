@model Emotify.Models.Emote

<dl class="row">
    <dt class="col-sm-2">
        @Html.DisplayNameFor(emote => emote.Id)
    </dt>
    <dd class="col-sm-10">
        @Html.DisplayFor(emote => emote.Id)
    </dd>
    <dt class="col-sm-2">
        @Html.DisplayNameFor(emote => emote.Names)
    </dt>
    @foreach (var name in Model.Names) {
        <dd class="col-sm-10">
            @Html.DisplayFor(_ => name.Name)
        </dd>
    }
</dl>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
